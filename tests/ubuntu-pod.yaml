apiVersion: v1
kind: Pod
metadata:
  name: ubuntu-pod
spec:
  runtimeClassName: kata-cc
  containers:
    - name: ubuntu-container
      image: ubuntu:latest
      command: ["tail", "-f", "/dev/null"]
